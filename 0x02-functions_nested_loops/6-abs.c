#include "main.h"

/**
 * _abs - Entry point
 * @n: the int to check
 * Description: 'the program's description'
 * Return: Always 0 (Success)
 */
int _abs(int n)
{
if (n >= 0)
{
return (n);
}
return (-n);
}
